diff --exclude-from=/home/dang/.scripts/diffrc -up -ruN bind-9.6.0.orig/configure.in bind-9.6.0/configure.in
--- bind-9.6.0.orig/configure.in	2008-10-29 06:08:58.000000000 -0400
+++ bind-9.6.0/configure.in	2008-12-28 14:22:14.000000000 -0500
@@ -2244,8 +2244,16 @@ main() {
 		[arch=x86_32])
 	;;
 	x86_64-*)
+AC_TRY_RUN([
+main() {
+	exit((sizeof(void *) == 4) ? 0 : 1);
+}
+],
+		[arch=x86_32],
+		[arch=x86_64
+		have_xaddq=yes],
 		have_xaddq=yes
-		arch=x86_64
+		arch=x86_64)
 	;;
 	alpha*-*)
 		arch=alpha
